# SillyTavern-ChatUI · Roadmap

Last updated: 2026-06-23

三份文档的分工:`DESIGN.md` = 产品北极星(目标形态)、`STATUS.md` = 当前实现快照、
**本文 = 完整度地图 + 剩余工作的优先级排期**。架构记录见 `ARCHITECTURE.md`。

---

## 完整度快照(对照 DESIGN 五大区)

| 区域 | 状态 | 已落地 | 还缺 |
|---|---|---|---|
| **地基** 架构重写 | ✅ ~完成 | shield→adapter→store→Preact 四层、旧 Phase1/2 清理、增量 store、流式实时、写路径加固、toast 层 | — |
| **①② 顶栏** | 🟡 ~25% | ☰ 召唤侧栏 + 标题 | 选择框槽 A、顶栏右对话操作 + ⋯ 更多(管理聊天文件/转群聊)、★ |
| **③ 内容区** | 🟢 ~80% | 角色整宽/用户气泡、操作行、思考块换皮、内联编辑、媒体、头像标头、swipe `‹n/m›`、代码复制 | 标头 3 档可配、代码块**语言名头**、**回到底部**钮、**底部重生成**钮 |
| **④ 输入框** | 🟢 ~70% | ＋菜单(内置+wand 动态)、textarea、选择框 B、发送/停止、附件 chips | **QR 悬浮条**、**单/多行切换**、＋菜单**配置** |
| **⑤ 侧栏导航中心** | 🔴 ~10% | 抽屉代理打开 ST 原生面板 | **整个导航中心**(见下方 M-A) |
| **配置系统**(§7 十项) | 🔴 ~0% | — | 一个设置界面都还没有(见 M-E) |

**毛估**:北极星完整度 ≈40%;"能当日常聊天用"体感 ≈70%(差 live-test 验证)。

---

## 已完成

- **地基**:原"重排 ST 原生 DOM"方案退役,改为 Preact 自渲染(shield 把原生 `#chat`/`#send_form` 移出屏幕只作回退面)。四层边界:UI → `actions` → `chat-actions` → adapter;adapter 是唯一碰 ST 内部的模块。
- **第 1 期 输入框**(主干):composer + ＋菜单 + 选择框 chips + 附件 chips。
- **第 2 期 内容区**(主干):消息流、操作行、思考块、编辑、媒体、swipe、代码复制。
- **B/D**:store 增量更新 + 流式实时。**H1**:ChatUI 自有 toast 反馈层。

---

## 剩余工作(按优先级)

### M-A · 三期 侧栏导航中心(region 5)— **当前攻坚目标**
DESIGN §2。最复杂、价值最高。分片交付:

1. **5c 对话列表(模式 A 单角色)** — DESIGN 标"先做"。`角色卡▾` 切角色 → `＋新对话` → 该角色对话按时间排。**最高频、最能立刻提升日常可用性,第一刀切这里。**
2. **5a 侧栏骨架/三形态** — 桌面常驻+折叠(① 横列表 / ② 方块 / ③ 纯 icon),手机覆盖式滑出。
3. **5b 上段配置图标** — 把原顶栏配置抽屉重新安家成图标(复用 ST `.drawer-content`,现 `ShellDrawer` 已是雏形)。
4. **5d 配置即浮层** — 桌面 (b)贴边/modal、手机全屏 tab + 返回。

### M-B · 顶栏右(region 2)
当前对话操作 + ⋯ 更多(管理聊天文件 / 转群聊)、选择框槽 A、★。

### M-C · 输入框收尾(region 4)
QR 悬浮条、单/多行切换、＋菜单配置(置顶磁贴 1~4 / 开关 / 拖拽)。

### M-D · 内容区收尾(region 3)
回到底部悬浮钮(并修当前"无脑滚到底打断看历史"的行为)、代码块语言名头、底部重生成钮、身份标头 3 档可配(群/单各一套)。

### M-E · 配置系统(贯穿 §7 十项)
统一设置界面:＋菜单工具、选择框槽 A/B、对话列表模式 A/B、侧栏三形态、配置面 modal、标头两套、操作行/消息菜单自定义、swipe 手势、单/多行。**所有"用户自配"叙事都压在这里**,目前全是硬编码默认。

---

## 阻塞项(待 live-test 后再动)

- **swipe→`swipe(forceMesId)`、delete→`deleteMessage`** 从模拟点击切到 ST 导出函数 — 有 null-event 运行时风险。
- **`content-visibility`** 双重渲染优化 — 等火焰图看 B/D 实际性能。
- **人设 chip 懒加载** — 现每次 sync 都拉 `getUserAvatars`,小优化。

---

## 当前分支

`cleanup/remove-legacy-phase12`(main 仍在基线 `c97d8c2`)。已提交 `c97d8c2`→`4426bdb`,
全部 static-verified、**尚未 live-test**。
