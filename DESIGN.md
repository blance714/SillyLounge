# SillyTavern-ChatUI · 交互界面重制方案

> 把 SillyTavern 整套交互（菜单栏 / 侧栏 / 输入框 / 内容区）重制成 ChatGPT / Claude 那类现代 AI 对话 App 的质感。
> **借鉴思路，不 1:1 照搬**：交互模式参考成熟 App，视觉用 ST 自己的主题色、内容是 ST 自己的功能。

> 📌 这是**产品北极星 / 目标形态**，不是当前实现清单。当前已落地的是 Preact 根应用的消息流 + 输入框骨架；本文里的 ＋菜单、选择框槽、侧栏三形态、身份标头配置等仍属待实现项。实现现状见 `STATUS.md`，技术架构见 `ARCHITECTURE.md`。
> 文中提到的早期"重排 ST DOM / 复用现成 `.mes` 节点"做法（§6、§8）已退役，仅作设计思路参考。

---

## 0. 总原则

- **手机 / 电脑分开设计**，但尽量同构（同一套结构，差在渲染细节）。
- **能复用就不重写**：ChatGPT 的多数消息特性 ST 的 DOM 已有（思考块、编辑、复制、swipe、代码复制）。本插件 ≈ **重排版 + 换皮 + 补几个新部件 + 重新归位**，不是从零造。
- **作用域隔离**：全部包在 `body.chatui-active` 下，禁用插件即还原原版 ST，零侵入源码。
- **频率驱动可达性**：越高频的操作越浅；ST 配置比 GPT 多得多且常调，所以桌面保「1 击直达」，不学 GPT 埋两层。
- **配置不内联**：可配项（+菜单工具、操作行按钮等）统一在独立设置里编辑，不在弹窗里拖拽。

---

## 1. 五大区域总览

```
📱 手机                              🖥️ 电脑
┌────────────────────────────┐     ┌──────────────┬────────────────────────┐
│ ☰ [返回] [选择框A]    ★  ⋯ │     │ 侧栏(常驻)     │ [选择框A]          ★  ⋯ │
├────────────────────────────┤     │ 🔍搜索        │────────────────────────│
│                            │     │ ⚙ 配置图标... │                        │
│        内容区(消息流)        │     │ ───────────  │     内容区(消息流)        │
│                            │     │ [角色卡▾]     │                        │
├────────────────────────────┤     │ ＋新对话      │                        │
│ [QR 悬浮]                   │     │ 💬 对话A ✓   │────────────────────────│
│ ＋ ……输入…… [选择框B] [●]   │     │ 💬 对话B     │ [QR] ＋ …输入… [B] [●] │
└────────────────────────────┘     └──────────────┴────────────────────────┘
  ☰ 点开 → 侧栏覆盖式滑出              侧栏可折叠成 ②方块 / ③纯icon
```

| # | 区域 | 职责 |
|---|---|---|
| 1 | **顶栏左** | ☰ 菜单（召唤侧栏）；配置 tab 中时旁边出【返回】；选择框槽 A |
| 2 | **顶栏右** | 当前对话操作 + ⋯ 更多 |
| 3 | **内容区** | 消息渲染 |
| 4 | **输入框** | composer |
| 5 | **侧栏 / 导航中心** | 配置 tab + 对话列表（第 5 区，核心） |

---

## 2. 区域 5 · 侧栏 / 导航中心

### 2.1 形态与召唤

- **桌面**：常驻，可折叠。三形态：
  - **① 横列表**（icon+name 横排，单列）：上段配置 + 下段对话列表
  - **② 单列方块**（icon+name 竖排，单列）：**仅配置**，无对话列表
  - **③ 纯 icon**：**仅配置**，无对话列表
- **手机**：只有 **① 横列表**，点 ☰ 覆盖式滑出。
- **☰ 统一含义 = 召唤完整列表 ①**：桌面在 ②/③ 时点 ☰ 展开成 ①（露出对话列表）；手机点 ☰ 滑出 ①。

### 2.2 上段 = 配置图标（原顶栏 7 个配置抽屉，重新安家）

预设 · AI 配置 · API · 格式化 · 世界书 · 用户设置 · 背景 · 扩展 …（即原 `#top-settings-holder` 的配置类抽屉）

**点配置图标 → 打开对应面板（复用 ST 现有 `.drawer-content`，不重写）**：

| 平台 | 打开方式 | 返回对话 |
|---|---|---|
| 🖥️ 桌面 | **(b) 贴对话推开**，对话留在右侧可见 | 无需返回（对话没消失）。未来可切 **modal** 模式（用户自选） |
| 📱 手机 | **全屏 tab 接管**（普通 tab，无 ✕） | 顶栏 ☰ 旁的【返回】按钮 / 再开 ☰ 选别的 |

> **关键认知**：「打开配置」是一个独立的 **开 / 关 状态**，不参与「当前对话」的单选高亮 → 配置和对话**不抢、不撞、不割裂**。

### 2.3 下段 = 对话列表

- **排序**：按时间。
- **模式 A（先做）· 单角色**：列表顶端 `[角色卡 ▾]` 切角色 → `＋ 新对话`（给当前角色新建）→ 该角色所有对话按时间排列。
- **模式 B（以后做）· 全局**：所有角色所有对话，每行 `头像 + 对话名 + 角色 + 时间`。

```
模式A 单角色                       模式B 全局(later)
┌────────────────┐               ┌────────────────────┐
│ [角色卡 ▾]       │ ←切角色        │ 🎴 对话1 角色A 今天  │
│ ＋ 新对话         │ ←当前角色新建   │ 🎴 对话2 角色B 昨天  │
│ 💬 对话1   今天   │               │ 🎴 对话3 角色A 上周  │
│ 💬 对话2   昨天   │               └────────────────────┘
└────────────────┘                头像前缀, 跨角色
```

---

## 3. 选择框（槽位系统）

- **2 个槽**：**A = 顶栏左**（☰ 右边）、**B = 输入框内**。（顶栏中槽已砍）
- **可放**：预设 / 模型(连接) / 人设 / 空。**用户自配**。
- **顶栏右不放选择框**（留给对话操作）。
- 预设 / 人设在「选择框（快速切换）」与「配置面 / 侧栏 tab（完整管理）」各出现一次 —— **两个深度，不重复**。

---

## 4. 区域 4 · 输入框 composer

### 4.1 组件

`＋` · `文本框` · `选择框 B` · `发送 / 停止`。**QR 快捷回复悬浮在输入框上方**。

### 4.2 单行 / 多行（用户可切）

```
多行(选择框B在底栏)               单行(无选择框B)
┌──────────────────────┐        ┌──────────────────────┐
│ [QR 悬浮]             │        │ [QR 悬浮]             │
│ ……输入(多行)……       │        │ ＋ …输入… [选择框B?][●]│
│ ＋ [选择框B▾]   [●发送]│        └──────────────────────┘
└──────────────────────┘         单行时 选择框B → 移入 ＋ 菜单最上面
```

### 4.3 ＋ 菜单（两段，均可配）

```
📱 手机 = 半屏 bottom-sheet           🖥️ 电脑 = 普通弹出菜单
┌──────────────────────────┐
│ ✕   工具                   │
│ ┌────────┐ ┌────────┐     │  ← ① 置顶磁贴: 1~4 可配
│ │🔁 重生成│ │🗑 删除  │     │     默认 [重生成][删除]
│ └────────┘ └────────┘     │
│ ────────────────────────  │
│ ✏️ 续写              ⇅ ◉  │  ← ② 工具列表
│ 🎭 代笔              ⇅ ◉  │     每项: 开关◉ + 拖拽排序⇅
│ 📷相机 🖼图片 📎文件  ⇅ ◉  │     默认: 普通工具在前
│ ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄ │
│ 🎨生图 🔊朗读 🎲掷骰  ⇅ ◉  │     扩展工具在后(动态拉取)
└──────────────────────────┘
```

- **① 置顶磁贴**：1~4 可配，从工具列表「提升」上来；默认 `重生成 + 删除`。
- **② 工具列表**：开关 + 拖拽排序；普通工具在前、扩展工具在后（扩展随装的插件**动态**变）。
- **配置入口独立**（不在菜单内编辑）→ 见 TODO。

### 4.4 归位（关键：原 `#options` ☰菜单被彻底拆光）

| 原功能 | 新去向 |
|---|---|
| 新对话 | 侧栏（＋新对话） |
| 续写 / 代笔 / 附件 / 🪄扩展工具 | ＋ 菜单 |
| 作者注释 / CFG / Token 概率 | 配置面 |
| 单条删除 | 消息长按菜单 |
| 批量删除 | ＋ 菜单的「🗑删除」→ 进勾选模式 |
| 重生成 | 内容区（消息操作行 + 底部按钮）+ ＋菜单磁贴 |
| 管理聊天文件 / 转群聊 | 顶栏右 ⋯ |

---

## 5. 区域 3 · 内容区（消息渲染）

### A. 整体布局

- **角色消息** = 整宽纯文本；**用户消息** = 右侧气泡。
- **身份标头**（角色消息头上的「头像 + 名字 + 时间」）3 档，**群聊 / 单聊各配一套**：

| 档位 | 显示 | 群聊默认 | 单聊默认 |
|---|---|:---:|:---:|
| 有 icon | 头像 + 名字(+时间) | ✅ | |
| 仅名字 | 名字(+时间)，无头像 | | |
| 无 | 纯 ChatGPT，什么都没有 | | ✅ |

> 理由：群聊多角色要分清谁是谁 → 有 icon；绝大多数是单聊 → 干净无标头。

### B. 角色消息操作行

- **一直显示**（桌面 + 手机都常显一行）。
- 默认 `复制 · 重生成 · 编辑 · ⋯(overflow)`，可配（与 ＋菜单同一套配置逻辑）。

### C. 用户消息菜单

- 触发：📱长按气泡 / 🖥️ hover 冒 ⋯ + 右键。
- **平铺全显，无 overflow**：`复制 · 编辑 · 删除 · 分支 · 检查点 · 隐藏`。

### D. swipe 翻页

- 角色消息下方 `‹ 2 / 3 ›` 按钮，跟操作行同排。
- **不做滑动手势**（避免与滚动 / 侧栏冲突）；手势 = TODO。

### E. 思考块 Thinking

- 复用 `.mes_reasoning_details`，换皮成「已思考 21s」折叠条，默认折叠。

### F. 代码块

- 复用 `.code-copy`，加一条头：`语言名(左) + 复制(右)`。

### G. 全局两个按钮

- **回到底部**：右下角悬浮圆钮，滚上去时出现。【新建】
- **底部重生成**：最后一条非 bot 消息时，底部冒「生成 / 重生成」。【已定】

---

## 6. 复用 ST 现有资源（重要：少造轮子）

| 新 UI 需要 | ST 现成 | 处理 |
|---|---|---|
| 配置面板内容 | 顶栏 7 个 `.drawer-content` | 重新安家，换皮，不重写 |
| 思考块 | `.mes_reasoning_details` | 换皮 |
| 代码块复制 | `.code-copy` (`addCopyToCodeBlocks`) | 加语言头 |
| 消息操作 | `.mes_buttons` / `.extraMesButtons` / `.mes_edit` | 重组成行 + 菜单 |
| swipe | `.swipe_left/right` `.swipes-counter` | 换皮成 `‹n/m›` |
| 附件 | `#file_form` | 进 ＋ 菜单 |
| 扩展工具 | 🪄 `#extensionsMenu`(wand) | 动态拉进 ＋ 菜单 |
| 快捷回复 | `#qr--bar` | 悬浮到输入框上方 |
| 群聊头像 | `.avatar_collage` | 身份标头用 |
| 事件 | `eventSource` / `event_types` | 监听消息渲染 / 切换对话挂钩 |

---

## 7. 配置 TODO 清单（可配项，放哪 / 怎么配 后续统一安置）

- [ ] ＋ 菜单工具配置（置顶 1~4 / 开关 / 拖拽排序）—— 独立设置界面，放哪儿待定
- [ ] 选择框槽 A / B 各放什么（预设/模型/人设/空）
- [ ] 对话列表 模式 A / B 切换
- [ ] 侧栏三形态切换、桌面常驻/折叠
- [ ] 桌面配置面板 (b)贴边 / modal 切换
- [ ] 身份标头：群聊{有icon/仅名字/无}、单聊{...} 两套
- [ ] 角色消息操作行 可见按钮自定义
- [ ] 用户消息菜单 项目自定义
- [ ] swipe 滑动手势（以后想要再加）
- [ ] 单行 / 多行 切换

---

## 8. 插件架构

- **新插件** `SillyTavern-ChatUI`（独立于早先的 `SillyTavern-Sidebar`）。
- **构成**：大型 CSS 主题（重排版 + 换皮）+ JS（新部件：顶栏 / 侧栏 / composer 重组 / ＋菜单 / 消息重排 / 回到底部 / 上下文菜单 / 配置即浮层）。
- **隔离**：`body.chatui-active` 作用域；禁用即还原。
- **响应式**：`@media` 分桌面 / 移动两套。
- **挂钩**：`eventSource.on(event_types.APP_READY / CHARACTER_MESSAGE_RENDERED / CHAT_CHANGED, …)` 等。
- **长期技术目标**：见 `ARCHITECTURE.md`。当前 Phase 1/2 仍以重排 ST DOM 为主；后续迁移到 `ST DOM Shield → ST Adapter → ChatUI Store → ChatUI UI` 的分层结构。

---

## 9. 分期实施建议

| 期 | 区域 | 理由 |
|---|---|---|
| **1** | 输入框 composer | 最独立、最快出效果，先验证整套架构 |
| **2** | 内容区 messages | 视觉冲击最大（气泡/标头/思考块/代码头/菜单/回到底部） |
| **3** | 侧栏 + 顶栏导航 | 最复杂（配置抽屉重新安家 + 对话列表 + 三形态），放最后 |

> 每期独立可跑、独立可看效果。可配项先用合理默认硬编码，待「配置 TODO」统一做设置界面时再接上。
