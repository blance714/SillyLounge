# SillyTavern-ChatUI · 长廊剧场设计规范

> ChatUI 的视觉北极星是一座只为一位读者开门的水墨戏楼：书脊上排开候场的角色，
> 场刊里列着他们演过的戏码，舞台上是一册仍在书写的私人手记。安静、克制、适合
> 长文阅读，以竖排点题、装订虚线、宣纸浮层、朱砂危险色、印玺状态和账本输入行
> 为主要语法。
>
> 本文是产品与视觉的唯一目标规范。当前实现见 `STATUS.md`，剩余工作见
> `ROADMAP.md`，技术边界见 `ARCHITECTURE.md`。早期“仿现代 AI App”、右侧用户
> 气泡、三形态折叠 rail 循环、第三列配置面板及重排原生 ST DOM 的方案均已退役。
>
> **2026-07-31 方向切换**：视觉身份由 Manuscript Flow 转为长廊剧场（corridor
> theater）。手记不是被丢掉，而是退回它真正管的地方——舞台内部的排版语法；
> 换掉的是舞台之外的导航结构。凡本文与旧 Manuscript Flow 条款冲突处，以本文
> 为准；下文各处标注「2026-07-31 拍板」的条款是已决事项，不再重开讨论。

---

## 1. 设计立场

- **先是阅读器，再是聊天工具**：消息正文拥有最高视觉权重，控件退到背景。
- **舞台不堆卡片，索引才用卡片**：阅读面依靠排版、留白与规则线组织，不靠圆角
  容器；场刊（对话列表）是索引面，允许卡片与装订线，但这套语法不得越界进舞台。
- **内容来自 SillyTavern，表面属于 ChatUI**：ChatUI 以 Preact 自渲染可见界面，
  不把原生 ST DOM 当作视觉结构。
- **手机与电脑同一种语言**：断点只改变排布，不改变信息层级与视觉语法。
- **状态必须来自真实宿主事实**：动效表达现有状态，不用延时或装饰性循环猜状态。
- **功能面可以有实体表面**：菜单、弹窗、设置面板可使用背景、边框与阴影；这些
  不是主阅读面的默认语法。

## 2. 视觉身份

### 2.1 色彩

| 角色 | 默认值 | 用途 |
|---|---:|---|
| 墨色地面 | `#16130f` | 舞台与主阅读面 |
| 书脊地面 | `#0f0d0a` | spine |
| 深纸表面 | `#1c1813` | playbill、深色浮层 |
| 象牙正文 | `#e8dfce` | 主要文字 |
| 发丝线 | `#2c2620` / `#3f372c` | 页面结构、区域边界 |
| 旧黄铜 | `#c9a06a` | 角色名、小标题、焦点 |
| 锈铜印记 | `#96633f` | 用户页边、激活描边、印玺 |
| 宣纸面 | `#b9ab8c → #a89877` | 菜单与确认弹窗，纸上文字 `#1c140b` |
| 朱砂 | `#8c3a2a` / `#7a2f1f` | 纸面上的破坏性动作 |

颜色必须以语义 token 使用，组件不得各自发明相近的棕、金、红。深色面上的危险
色与纸面上的朱砂是两档不同的值，不得互相顶替。

### 2.2 三层规则线

规则线不是同一个 `border` 到处复用：

1. **淡线 `--cui-rule-faint`**：正文 connector、思考边注、场刊内部分隔。
2. **结构线 `--cui-rule-structure`**：topbar、spine/playbill、composer 等区域边界。
3. **强调线 `--cui-rule-accent`**：用户页边、当前对话、活跃书写区域。

强调线必须稀少；若页面上每条线都发亮，就失去层级。

### 2.3 字体

- 会话标题、角色名、消息正文、思考正文与输入文字使用衬线字体。
- 按钮、时间、状态、设置项等功能文字使用系统无衬线字体。
- 代码、swipe 计数、楼层号等「要被当作数字读」的文字使用等宽字体。
- 字号从 `--cui-font-root` 这一个基准派生，不读宿主 `--mainFontSize`：宿主变量
  未定义时整条 `calc()` 会失效，属于 fail-unsafe。
- 正文桌面目标行高约 `1.8`，手机约 `1.7`；代码块单独收紧。
- **字面目标是 `Noto Serif SC`，但暂缓自托管，接受系统衬线回退**
  （2026-07-31 拍板）。回退链必须覆盖 macOS / Windows / Android 三端的中文衬线，
  各端观感有差异是已知且接受的代价；不引入任何在线字体服务（见 §9 不做清单）。
- 将来若改为自托管，成本不是「加一个 woff2」：`scripts/runtime.mjs` 的
  `RUNTIME_PATHS` 是硬编码白名单、`scripts/check-runtime.mjs` 逐文件校验运行时树，
  改动构建契约需同步补测试与 `INVARIANTS.md` 登记。这是立项级改动，不是顺手项。

### 2.4 动效

- 常驻动效只保留生成态印玺的缓慢呼吸。
- hover、focus 和抽屉切换应短而轻，不做弹跳或大幅缩放。
- 入场动画收敛为少数几个具名 keyframes（浮层淡入、遮罩淡入、弹窗入场、印玺呼吸），
  不为单个组件另发明一套时长与缓动。
- 流式输出只做整块淡入，不做逐字符动画（原因见 §9 不做清单）。
- `prefers-reduced-motion: reduce` 下印玺保留静态状态，但停止动画；新增的每一个
  动画都要一并进这条查询，不能只覆盖印玺一个。

### 2.5 间距与尺度

- 目标是像素级还原：间距、圆角、控件尺寸按设计稿的绝对值标定。
- 现存 `rem` 间距的基准是宿主页面 `<html>` 的字号，宿主不是 `16px` 时整套节奏会
  整体缩放——字号已经脱钩宿主，间距是同一件事没做完的后半段。
- **处置方式（2026-07-31 拍板）：随各区域换肤逐区把间距/尺寸从 `rem` 收敛为 `px`**，
  一个区域一次改干净，不做一次性全局替换（全局替换既无法验收也无法回滚）。
  已收敛的区域不得再退回 `rem`，未收敛的区域不新增 `rem` 间距。

## 3. 页面结构

```text
桌面（spine 58px │ playbill 252px │ stage 自适应）     手机
┌────┬────────────┬──────────────────┐  ┌──────────────────────┐
│书脊│ 角色名 · N │ 角色名 · eyebrow │  │ ☰ 角色名        人设 ⋯ │
│ ◉  │ ＋ 新对话  │ 会话题名         │  │    会话题名            │
│ ○  ├────────────┼──────────────────┤  ├──────────────────────┤
│ ○  │┌──────────┐│                  │  │                      │
│ ＋ ││对话卡片 ┃││ 刻度 │ 阅读流    │  │       阅读流          │
│    │└──────────┘│                  │  │                      │
│    │ 其他对话   ├──────────────────┤  ├──────────────────────┤
│ ⚙  │ 未完成草稿 │ ＋ 写下这一夜…… →│  │ ＋ 写下这一夜……     → │
└────┴────────────┴──────────────────┘  └──────────────────────┘
                                          ☰ 打开的抽屉 = spine + playbill
```

- 桌面是固定的 `Spine | Playbill | Stage` 三栏（2026-07-31 拍板）：书脊 `58px`、
  场刊 `252px`，两栏都常驻、不折叠、不循环形态。
- 这不是已退役的「三形态折叠 rail」的复活：那个方案是同一栏在三种形态间循环，
  这里是两条职责固定的栏。spine 只回答「现在是谁」，playbill 只回答「哪一场」，
  stage 只负责读与写；任何一栏都不代另一栏答题。
- 手机（`≤768px`）把 spine 与 playbill **一并**收进同一个覆盖式 drawer，抽屉内
  保持「窄书脊 + 场刊列」的同一结构，不拆成二级页面（2026-07-31 拍板）。
- 设置模式仍是整屏 mode swap：设置导航与内容接管 playbill 与 stage，spine 保持
  常驻以便随时回到某个角色；不在聊天面上叠加第四列。
- 只有消息列表滚动；topbar 与 composer 固定在阅读面两端。

### 3.1 三栏宽度与阅读列宽度是一个联立方程

**spine 的宽度与阅读列的宽度必须在同一个改动里标定，不能分两次做**（2026-07-31
拍板）。理由是楼层轨的**存在性**本身就是布局宽度的函数：

- `MessageFloorRail.tsx` 的 `hasReadingGutter` 判定是整条轨的生死开关——只有
  `.cui-root-message-list` 左缘与轨道左缘之间真实留出 `30px` 轨宽 + `12px` 正文
  间隔，轨才挂载，否则直接不渲染。
- 按现行 `--cui-content-max-width: 54rem` 计算，楼层轨大约要 `1252px` 视口才出现；
  `1280px` 笔记本目前只剩约 `28px` 余量。
- 再加 `58px` spine 而不同步收窄阅读列，临界值抬到约 `1310px`——**楼层轨会在
  1280 屏上静默消失**：不报错、不挂测试、不打红 CI，只是功能不见了。

因此：采用 spine 就等于同时接受「重新标定阅读列宽度」。设计稿主列 `680px` 是
收窄的参考基准，最终值以「加入 spine 后 `1280px` 视口楼层轨仍然挂载」为验收条件
（§8）。谁改 spine 谁负责这条标定。

## 4. 四个签名区域

### 4.1 Title-page topbar

- **eyebrow = 角色名**，小号、克制、无衬线。
- **主标题 = 会话名**，衬线、完整显示为页面题名。
- 两层数据必须独立呈现，不得再写成 `characterName || sessionName` 而遮掉其一。
- 没有会话名时，主标题可以回退到角色名或 `ChatUI`，同时避免重复题名。
- persona 与对话操作位于右侧工具区，视觉权重低于标题。
- 窄屏可把 persona 收成仅图标按钮，但必须保留可访问名称。
- topbar 内的下拉向页面下方打开，不得被根容器裁切。

### 4.2 书脊 spine 与场刊 playbill

侧栏由两栏组成：spine 是角色的候场区，playbill 是当前角色的场刊。二者共同取代
旧的单栏「手记索引」（2026-07-31 拍板）。

**书脊 spine（`58px`）**

- 只放三样东西：角色头像方块（`32×32`、圆角 `8px`）、导入/新角色的虚线「＋」、
  底部设置齿轮；中段竖排店名用来点题，不是装饰填充。
- 头像是定位符不是主角：非激活态降不透明度，激活态只用一圈锈铜描边表示，不用
  实底高亮块、不用尺寸放大。
- spine 是「切换角色」的唯一常驻入口，但它**不是联系人列表**：没有名字行、没有
  未读数、没有最近消息预览、没有在线状态。角色的名字属于 playbill 顶部与 topbar。
- **群聊在 spine 占一个独立槽位**，用组图标表示（不用任一成员的头像冒充）；
  选中群组后 playbill 维持现有「群聊对话列表即将支持」的降级提示。完整群聊支持
  另行立项，不在换肤批次里顺手做（2026-07-31 拍板）。
- composer 里的横向选角条 `NewChatCharacterPicker` **保留**：它的职能是「新建对话
  时选角色」，spine 的职能是「切换当前角色」。两者不合并、不互相替代；同屏出现
  两处角色头像是可接受的，因为它们回答的是不同的问题。若将来要合并，先改本文
  （2026-07-31 拍板）。

**场刊 playbill（`252px`）**

- 列的是**当前角色**的对话，顶部是「角色名 + 的对话 · N」，底部是「＋ 新对话」。
  它是一份场刊，不是全局收件箱，也不做跨角色的合并时间线。
- 对话是卡片：小圆角（`3px` 量级）、左侧 `14px` 竖排装订区、装订区右缘 `1px dashed`。
  卡片在这里是被允许的语法——它是索引面，不是舞台。
- 当前对话用锈铜边框与锈铜装订虚线标识；**不使用高对比实底选中块**，也不使用
  会与舞台正文抢视觉权重的大色块。
- 操作坞（改名、删除）在 hover 时才浮出卡片右上角；改名是就地 input，`Enter`
  保存、`Esc` 取消。
- 未完成草稿卡内嵌在同一列，用虚线边框与「未完成草稿」标签区分，hover 出丢弃
  入口。它必须继续走既有的 quarantine lease 机制：视觉重排不得把草稿混进普通历史，
  也不得绕过 lease 判定去删文件。
- 场刊列表独立滚动；顶部标题与底部「＋ 新对话」不随之滚走。

### 4.3 阅读流 messages

- 角色消息与用户消息都属于开放式阅读流，不使用左右聊天气泡。
- 两位说话人各有一条左侧页边线，颜色不同、缩进相同：用户是锈铜，角色是结构灰。
  同一条缩进是「长廊」读感的来源——两个声音是一列，不是两列错开。
- 标头行是「作者名 ── 连接线 ──「第 N 楼 · 时间」」：作者名取行高本身的 `11px`，
  角色为金棕、用户为另一暖色；楼层与时间是一个 `10px` 的整体印记，不是三个各自
  游离的字段。
- **单聊标头默认值是 `name`**（2026-07-31 拍板）。旧默认 `none` 意味着最常见的
  场景根本不渲染这一母题——需要用户手动打开的母题不算设计。群聊默认仍是 `icon`
  （靠头像区分发言者）；设置页三档 `icon / name / none` 不变，想要纯净列的读者
  仍可切回。
- 楼层号随消息视图模型下发，与楼层轨用同一套用户回合编号；开场白、系统消息、
  群聊同一回合的第二位以后的回复没有楼层号，标头就不渲染那一段——不补编号。
- 桌面消息间距目标约 `26px`，手机约 `16px`（按 §2.5 以像素标定，不随宿主字号漂移）。
- 思考内容是虚线、斜体的页边注；代码、媒体等功能内容可使用实体边界。
- 消息操作默认隐退，在 hover、focus 或触摸场景下可达。
- 舞台内侧的楼层轨可出现用户回合刻度：每条横线只代表一条用户消息，固定
  `2px` 高、`8px` 间距（`10px` 节距）；整叠刻度在阅读区垂直居中，并距主区左边缘
  约 `16px`。
- **刻度几何是门禁锁定量**：真实浏览器 smoke 按像素断言相邻刻度的 `10px` 节距。
  换肤只能改颜色、圆角与不透明度，不得改刻度高度/间距，也不得给刻度加边框。
- hover 时邻近刻度形成连续波形；浮层标题显示该条用户原话，正文显示紧随其后
  的角色回复（排除 reasoning/包装标签，最多三行），点击跳到该用户消息。
- 刻度过多时，上下各保留 `40px` 安全区，只渲染能容纳的一窗；阅读位置在底部
  时刻度窗也对齐末端。鼠标停在刻度上滚轮可翻看被截去的早/晚回合；翻动期间
  在轨道上、下短暂显示当前窗首末用户回合编号，停止后淡出。
- **楼层轨保留 ChatUI 自有的样式与交互**（2026-07-31 拍板）：居中限窗、滚轮翻窗、
  user→reply 浮层、垂直 slider 键盘语义、边缘淡出，全部按本节而非按设计稿实现。
  设计稿的楼层轨规格（`64px` 固定列、`280px` 右侧预览气泡）明确**不采用**——
  长廊剧场只给它换配色与字体。
- 只有侧栏与阅读列之间的真实留白足以容纳刻度、波浪和至少 `12px` 正文间隔时才
  显示；不得覆盖正文或伪装成第二条滚动条。阅读列最大宽度不是一个可以单独调的
  数，见 §3.1：它与 spine 宽度联立，现行 `54rem` 在加入 spine 后必须收窄，
  设计稿主列 `680px` 是参考基准。

### 4.4 Ledger composer

- composer 是开放的账本书写行，始终有结构横线，不是圆角输入胶囊。
- 焦点只提高规则线的强调层级，不生成新的盒子或阴影容器。
- placeholder 使用“写下这一夜……”；发送使用轻薄箭头，不使用高饱和实心按钮。
- ＋菜单、附件 chips、QR 与 selector 仍可正常展开，但不得破坏主横线。
- single / multi 两种模式沿用同一语法；多行增长不能挤走 topbar。

## 5. 印玺生成态

- 印玺只绑定 ChatUI 已有的真实 `isGenerating` 状态。
- 它位于消息阅读流末端，表示“下一笔正在落下”，而不是贴在 composer 上的状态胶囊。
- 形态为旧黄铜与锈铜之间的细环＋中心点，配一条真实状态文字（「正在酝酿……」）。
- 它带着与消息相同的标头行（作者名＋连接线）和相同的缩进，只是页边线透明：第一个
  token 落下时读者看到的是那一行文字被替换，而不是整段结构重新排布。
- 非生成态不渲染印玺；停止或结束生成后立即消失。
- 动画仅改变不参与布局的视觉属性，不能令消息或 composer 跳动。
- 不得把 `pendingSend`、首个 token 或任意 timer 当作生成状态替代品。

## 6. 交互契约

### Topbar、spine 与 playbill

- 顶栏 ☰ 只在手机出现，负责打开合并了 spine 与 playbill 的抽屉；桌面两栏常驻，
  没有折叠按钮。
- 顶栏 ⋯ 承载当前对话重命名与删除，破坏性操作必须确认。
- 在 spine 选角色、在 playbill 选对话，手机抽屉都自动关闭。
- 切换 spine 角色后，playbill 必须换成该角色的对话列表；不允许出现「头像已换、
  列表还是上一个角色」的中间态。
- 当前对话标识必须同时校验角色归属与会话文件名。

### 确认与浮层

- 破坏性动作一律走 ChatUI 自有确认弹窗（不用宿主原生弹层）：宣纸底 + 描边款
  危险确认钮，不用实底红按钮。
- **键盘模型（2026-07-31 拍板）**：焦点默认落在确认钮；`Enter` 确认，但弹窗出现
  后 `300ms` 内吞掉 `Enter`；`Esc` 取消。
- 这条**推翻**了旧的「取消钮自动聚焦」安全默认。新的安全性由两件事承担：`300ms`
  守卫挡住「在输入框连按回车顺手确认」，描边款危险钮在视觉上不诱导点击。
- 实现约束：同一次 `Enter` 不得既触发被聚焦按钮的原生 click、又触发窗口级监听。
  两个互斥回调在同一帧都跑是 bug，不是可以靠时序运气规避的竞态。
- 菜单互斥：打开任一菜单关闭其余；点击外部关闭全部。浮层向下打开为默认，空间
  不足时才翻转，且不得被根容器裁切。

### 消息

- 保留复制、重生成、编辑、删除、分支、检查点、隐藏与 swipe。
- 群聊和单聊的身份标头继续支持 icon / name / none 三档配置。
- 回到底部只在读者离开底部时出现；流式生成不得抢走正在阅读的滚动位置。
- 桌面用户回合刻度支持点击与键盘 `↑/↓`、`PageUp/PageDown`、`Home/End`；
  当前回合随真实滚动位置更新。发生数量裁切时滚轮翻动刻度窗，否则继续滚动正文；
  浮层离开轨道或嵌入式 HTML card 后立即收起。

### Composer

- `Enter` 发送，`Shift+Enter` 换行。
- 空输入时 `Ctrl/Cmd+Enter` 重生成，`ArrowUp` 编辑最后一条。
- `Escape` 优先退出编辑，其次停止生成。
- 草稿、附件与发送生命周期遵循 store/adapter 契约；视觉层不绕过这些入口。

## 7. 响应式与可访问性

- 关键验收宽度：`390px`、`480px`、`768px`、`769px`、`1280px`、桌面 `1440px`。
  `1280px` 是楼层轨显隐的临界区（§3.1），`480px` 是 topbar selector chip 收成
  纯图标的那一档。
- `≤768px` 时 spine 与 playbill 一并进抽屉（§3）；抽屉打开时舞台不横向位移出屏，
  遮罩点击关闭。
- 长角色名、长会话名必须省略，不得把 persona 或 ⋯ 推出屏幕。
- icon-only 控件必须有动态 `aria-label` 与 `title`。
- 生成态保留 `role="status"` 和可读文字，不能只靠颜色圆点。
- 用户回合刻度保留垂直 slider 语义、可读回合值与键盘操作；减弱动态偏好下取消
  刻度宽度动画，但不取消定位能力。
- 触摸端不得照搬 hover 楼层轨。移动端方案在单独完成防误触设计前保持隐藏；
  未来可以探索中置标尺/拖拽，但不能让普通上下滚动误触跳楼。
- 除这项已明确延期的移动楼层导航外，hover 功能在触摸设备上必须有等价入口。
- 文字与规则线在默认深色稿纸上保持可辨，不以降低可读性换氛围。

## 8. 验收清单

每次修改签名区域，至少核对：

- [ ] 普通桌面会话：双层题名、阅读行距、三档规则线。
- [ ] 正在生成：只有一个印玺，停止后消失。
- [ ] 多行 composer：横线、focus、＋菜单、selector、附件与 QR 不错位。
- [ ] `390px` 手机：☰、双层题名、persona、⋯ 无横向溢出。
- [ ] `768px / 769px`：抽屉（spine + playbill 合并）与常驻三栏在断点两侧都正确。
- [ ] 桌面长对话：刻度数量等于用户消息数；`2px/8px` 节奏（`10px` 节距）、居中、
      16px 左距、user→AI 浮层、点击/键盘跳转与 HTML card 离开都正确。
- [ ] 超长对话：刻度窗上下安全区约 40px；底部阅读对齐末端，hover + 滚轮可
      翻看被裁切的早/晚用户回合且不带动正文；首末编号随窗更新并在停止后淡出。
- [ ] `390px / 768px`：不出现桌面楼层刻度，也没有透明热区或正文缩进。
- [ ] 窄桌面：阅读列仍按正常宽度工作；书脊留白不足时整条刻度隐藏，不覆盖正文。
- [ ] `1280px` 桌面：加入 `58px` spine 后楼层轨仍然挂载（阅读列已按 §3.1 收窄）。
- [ ] 当前对话：playbill 用锈铜描边＋装订虚线标识，不出现高对比实底选中块；
      spine 激活态只用描边，不出现名字行或未读数。
- [ ] 群聊：spine 有独立组图标槽位；选中后 playbill 给出降级提示而不是空白。
- [ ] 删除确认：焦点在确认钮、弹出 `300ms` 内按 `Enter` 无效、`Esc` 取消，
      且同一次 `Enter` 只触发一个回调。
- [ ] `prefers-reduced-motion`：印玺静止、楼层波形不做过渡，但状态与定位仍清楚。
- [ ] 长消息、代码块、HTML card：增大正文行距后没有异常撑高或裁切。

## 9. 防漂移边界

以下改动视为视觉回归，除非先更新本文并说明理由：

- 把用户消息改回右侧圆角气泡。
- 把 composer 改回带背景和阴影的输入胶囊。
- 把 topbar 压回只有一个 fallback 标题的工具行。
- 用 spinner/pill 替代生成印玺，或忽略 reduced-motion。
- 把三栏塌回单栏侧栏，或给 spine 加上名字行、未读数、最近消息预览——即把书脊
  变回头像驱动的联系人列表。
- 给 playbill 卡片加高对比实底选中块，或把卡片/圆角 chip 语法带进舞台
  （阅读流、topbar、composer 出现卡片容器）。
- 加 spine 而不同步标定阅读列宽度，导致楼层轨在 `1280px` 静默消失（§3.1）。
- 将所有规则线合并成同一颜色和权重。
- **把消息正文里的引号（`q`）与强调（`em`）交还给宿主主题**。正文元素带着 ST 的
  `.mes_text` 类，不接管就等于让对白颜色随用户装的 SillyTavern 主题变——舞台上
  最不该漂移的正是对白。这两个颜色归 ChatUI 管，取自长廊剧场调色板，用 token
  引用而非裸值。
- 把单聊标头默认值改回 `none`，或以「更干净」为由让签名母题默认不渲染（§4.3）。
- 为了视觉方便，让 UI 直接读取 ST DOM 或复制宿主状态。

### 9.1 不做清单

以下四条不是口味问题，是架构事实或宿主边界。它们不随设计稿更新而松动；要做必须
先在本文推翻，并说明代价由谁承担：

- **逐字符 `chunk-in` 流式动画**：消息正文是 ST formatter 产出的整块 HTML，经
  `dangerouslySetInnerHTML` 注入，Preact 侧根本没有可以逐字符包裹的节点树。要做
  必须先自建 HTML 分词器（会切断标签与实体）、放弃 `dangerouslySetInnerHTML`、
  重写依赖「完整 DOM 已就位」的挂载 effect，并打掉流式重渲染按引用相等比较的
  颗粒度承诺；同时把已挂载元素数从「可见行数」放大到「可见字符数」，直撞性能
  红线。流式只做整块淡入。
- **全局 `scroll-behavior: smooth`**：贴底逻辑是裸 `scrollTop = scrollHeight`
  赋值，会被 CSS 全部动画化；而虚拟化库走 `scrollTo({behavior:'auto'})` 仍是瞬时。
  两者在同一个滚动容器上互相打断。需要平滑时按调用点传参，并尊重
  `prefers-reduced-motion`。
- **任何外部 CDN**：Google Fonts、cdnjs 的 Font Awesome、`cdn.simpleicons.org`
  的模型图标一律不引。ChatUI 是注入宿主页面的扩展，外部 `<link>` 与动态拼接的
  资源 URL 既是 CSP 风险，也是跨地区可用性风险。图标要么本地内嵌 SVG，要么退化
  为已有图标集。
- **全局 `::-webkit-scrollbar`**：滚动条样式只允许 scope 到 `.cui-*` 容器。全局
  规则会改掉 SillyTavern 自己所有滚动条——那不是我们的表面。
